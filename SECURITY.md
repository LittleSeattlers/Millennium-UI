# Security and privacy

Please report a potential secret, identity, local-path, execution-log, or telemetry disclosure privately to the repository owner rather than opening a public issue containing the data.

The public dashboard is designed to fail closed around a strict aggregate schema. If private data is accidentally committed, revoke affected credentials when applicable, remove the material from the public branch, and treat Git history and downstream caches as already exposed.
